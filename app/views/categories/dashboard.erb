<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Avalon&#39;s Altar</title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Poiret+One">
    <link rel="stylesheet" href="stylesheets/metrics.css">
    <link rel="shortcut icon" type="image/x-icon" href="/images/om-favicon.ico">
  </head>
  <h1>Category Metrics</h1>
    <p>Average Item price for <%= @categories[0].name%>: $<%= @itemscategory1.average_price_per_item %></p>
    <p>Average Item price for <%= @categories[1].name%>: $ <%= @itemscategory2.average_price_per_item %></p>
    <p>Average Item price for <%= @categories[2].name%>: $ <%= @itemscategory3.average_price_per_item%></p>
    <p>Average Item price for <%= @categories[3].name%>: $ <%= @itemscategory4.average_price_per_item%></p>
    <p>Average Item price for <%= @categories[4].name%>: $ <%= @itemscategory5.average_price_per_item%></p>
    <p>Category with Most Expensive Item: <%= @categories[@most_expensive_id - 1].name %></p>
    <p>Category with Least Expensive Item: <%= @categories[@least_expensive_id - 1].name %></p>

</html>
